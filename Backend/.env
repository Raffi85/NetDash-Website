# Flask Configuration
FLASK_APP=app.py
FLASK_ENV=development
SECRET_KEY=your_very_secret_and_long_random_key_here

# Database Configuration
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=P@ssW0rd
DB_NAME=product_website

# Email Configuration (SMTP)
# Gmail example (you'll need to enable "Less secure app access" or use app passwords)
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-email-password-or-app-password

# Alternative email providers:
# Yahoo: smtp.mail.yahoo.com, port 587 or 465
# Outlook: smtp-mail.outlook.com, port 587
# Custom SMTP servers can be configured similarly

# For production, use environment-specific SMTP settings
# and consider using services like SendGrid, AWS SES, etc.